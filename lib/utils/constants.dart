enum UploadType {
  GALLERY,
  CAMERA,
}

enum PostCaptionOpenMode {
  FROM_POST,
  FROM_FEED,
}

enum FeedMode {
  FROM_FEED,
  FROM_PROFILE,
}

enum PostMenu {
  EDIT,
  DELETE,
  SHARE,
}

enum ProfileMode {
  MYSELF,
  OTHER,
}

enum ProfileSettingMenu{
  THEME_CHANGE,
  SIGN_OUT,
}

enum WhoCaresMeMode{
  LIKE,
  FOLLOWERS,
  FOLLOWINGS,
}
